require 'spec_helper'

feature 'manager edits schedule set', %q{
  As a manager
  I want to be able to edit already made schedules
  So that I can update them with new changes
} do

  # Schedules are loaded with existing values
  # When new values are selected and saved, the database is updated appropriately

  pending ""

end
